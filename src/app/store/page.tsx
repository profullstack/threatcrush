import StoreContent from "./store-content";

export const metadata = {
  title: "Module Store — ThreatCrush",
  description: "Browse and install security modules for ThreatCrush.",
};

export default function StorePage() {
  return <StoreContent />;
}
